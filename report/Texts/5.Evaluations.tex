\section{NIST randomness test suite}\label{sec:NIST}
\todo[inline, color=purple]{section - empirical comparison / Implementation comparison}
\todo[inline]{This first paragraph is maybe not suited...}

Quantifying unlinkability in anonymous communication systems remains inherently challenging.
Loopix~\cite{piotrowska2017loopix}, for instance, introduces a probabilistic metric called the \emph{expected difference in likelihood} to evaluate sender-receiver unlinkability.
However, that metric relies primarily on network-level simulations and focuses on the probability that a given packet originates from a particular client.
In contrast, our goal is to assess unlinkability at the \emph{mixnode level}, i.e. the difficulty for a global passive adversary (GPA) to correlate an incoming packet with its corresponding outgoing packet through cryptanalysis.
Assuming that higher entropy and statistical randomness hinder such correlations, we empirically evaluate the bit-level randomness of our packet headers.
%
\subsection{Test suite overview}
To evaluate the statistical quality of our generated headers, we employ the NIST SP 800-22 statistical test suite~\cite{NIST-SP80022}.
This suite comprises 15 tests designed to assess whether a binary sequence exhibits properties expected from a truly random source.
Each test produces a \emph{p-value}, which measures how likely the data could be produced by a uniform source. 
If headers are truly random, the distribution of p-values over many samples should be statistically uniform.

Below we summarize the tests most relevant to our analysis:
\begin{itemize}
    \item \emph{Frequency Test}:
    Evaluates the balance between zeros and ones across the sequence. 
    A uniform random bitstring should contain roughly equal proportions of each.

    \item \emph{Matching Tests (Overlapping / Non-overlapping)}: 
    Examine how frequently predefined $m$-bit patterns (with default $m=9$) appear in the sequence.
    The non-overlapping version avoids reusing bits after a match, while the overlapping version slides the window by one bit at a time, capturing local repetition patterns.

    \item \emph{Random Walk Tests}:
    Interpret each bit as a step in a random walk ($+1$ for one, $-1$ for zero) and analyze its deviation from the expected mean behavior.
    The {cumulative sums} test checks how far the walk strays from zero.
    The {random excursions} and {random excursions variant} tests count how often specific states are visited, either per cycle\footnote{A cycle is a segment of the walk between two successive zero states, i.e. states where the cumulative sum equals $0$.} or across the entire sequence.
\end{itemize}
In short, while the \emph{Frequency} test verifies global balance, the \emph{Matching} tests detect hidden repetitive structures, and the \emph{Random Walk} tests analyze the dynamic balance of bits over time.


\subsection{Evaluation methodology}

We compare our decentralized implementation against the original Sphinx implementation by Danezis\footnote{\href{https://github.com/UCL-InfoSec/sphinx}{https://github.com/UCL-InfoSec/sphinx}} using identical NIST SP 800-22 configurations.

Twenty independent datasets were generated for each implementation.
Each dataset was produced from a simulated network of 20 mixnodes and 3 semi-trusted third parties (STTPs).
For each trial, a random destination IP and a path of three mixnodes were selected.
The client generated additive shares and distributed them to the STTPs, which then independently computed and aggregated partial headers before forwarding them through the simulated mixnet.
%
According to NIST requirements, each input sequence should contain at least $1\,000\,000$ bits.
We therefore concatenated multiple generated headers to reach a sequence of this size, repeating the process to obtain 100 sequences per dataset.
\newline


NIST recommends interpreting results via two complementary criteria:
\begin{itemize}
    \item Proportion of passing sequences (Figure~\ref{fig:proportion});
    \item Uniformity of p-value distributions (Figures~\ref{fig:hist_p-values}-\ref{fig:chisquare}).
\end{itemize}


\subsection{Passing rate analysis}

Figure~\ref{fig:proportion} highlights the proportion of sequences that passed each NIST test for both implementations.
Following NIST guidelines, the significance level is set to $\alpha = 0.01$, and the confidence interval over $m$ samples is defined as:
$$
(1 - \alpha) \pm 3 \sqrt{\frac{\alpha (1 - \alpha)}{m}},
$$

Our implementation fails only three tests: \textit{Non-overlapping Matching}, \textit{Random Excursions}, and \textit{Random Excursions Variant}, all of which are also failed by the original Sphinx implementation.  
In contrast, our implementation passes three tests that the original implementation fails: \textit{Frequency}, \textit{Cumulative Sums}, and \textit{Overlapping Matching}.
\begin{figure}%[b]
    \centering
    \includegraphics[width=1.\linewidth]{Images/proportion_dot.png}
    \caption{Proportion of sequences passing each NIST test for both our implementation and the original Sphinx (significance level = 0.01).}
    \label{fig:proportion}
\end{figure}


\subsection{P-value distribution analysis}

\todo[inline]{Figure \ref{fig:hist_p-values} feels too much, maybe better to put it in appendix ?}
Figure~\ref{fig:hist_p-values} shows the p-value histograms for both implementations across all 15 NIST tests.
Uniformity of these distributions is further assessed using a Chi-square goodness-of-fit test with a significance threshold of $0.0001$, as recommended by NIST.
Figure~\ref{fig:chisquare} presents these results, highlighting that our implementation achieves uniform p-value distributions on the \textit{Frequency} and \textit{Cumulative Sums} tests, whereas the original Sphinx fails them.

\begin{figure}%[H]
    \centering
    \includegraphics[width=1.\linewidth]{Images/hist_p-values.png}
    \caption{Distribution of p-values for both implementations across all the NIST SP 800-22 statistical tests.}
    \label{fig:hist_p-values}
\end{figure}

\begin{figure}%[H]
    \centering
    \includegraphics[width=1.\linewidth]{Images/chisquare.png}
    \caption{Chi-square test assessing the uniformity of p-value distributions for each dataset across all the NIST SP 800-22 statistical tests.}
    \label{fig:chisquare}
\end{figure}


\subsection{Discussion}

In terms of output randomness, our decentralized implementation performs better than the original Sphinx, as it passes a greater number of statistical tests, as summarized in Table~\ref{tab:test_summary}.
The results suggest that both implementations may exhibit subtle patterns or hidden structure, as indicated by failures in the \textit{Random Excursions} and the \textit{Non-overlapping Matching} tests. 
This behavior could potentially come from the underlying mathematical framework (e.g. elliptic curves), which is not inherently truly random.
Nevertheless, our implementation demonstrates a better overall balance between ones and zeros, as reflected in the \textit{Frequency} and \textit{Cumulative Sums} test results.
Given the established security of the original Sphinx protocol, and considering the similarities in core structure, our results indicate that our scheme is a solid starting point toward achieving similar privacy guarantees.

\begin{table}%[t]
\caption{Statistical test summary for both implementations (\CIRCLE~=~Pass, \hspace{0.5mm}\Circle~=\enspace Fail)}
\label{tab:test_summary}
    \scalebox{.85}{
    \begin{tabular}{|l|cc|cc|}
    \toprule
                                    & \multicolumn{2}{c|}{\textbf{Decentralized}}   & \multicolumn{2}{c|}{\textbf{Original}} \\
    \cline{2-5}
                                    & \textbf{\footnotesize Excessive}    & \textbf{\footnotesize Lacks}        & \textbf{\footnotesize Excessive} & \textbf{\footnotesize Lacks} \vspace*{-0.8mm} \\
    \textbf{Statistical Test}       & \textbf{\footnotesize Rejections}   & \textbf{\footnotesize Uniformity}   & \textbf{\footnotesize Rejections} & \textbf{\footnotesize Uniformity} \\
    \midrule
     \small Frequency                       & \CIRCLE               & \CIRCLE               & \Circle            & \Circle     \\
     \small Block Frequency                 & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
     \small Cumulative Sums                 & \CIRCLE               & \CIRCLE               & \Circle            & \Circle     \\
     \small Runs                            & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
     \small Longest Run                     & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
     \small Rank                            & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
     \small Discrete Fourier Transform      & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
     \small Non-overlapping Matchings       & \Circle               & \CIRCLE               & \Circle            & \CIRCLE     \\
     \small Overlapping Matchings           & \CIRCLE               & \CIRCLE               & \Circle            & \CIRCLE     \\
     \small Universal Statistical           & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
     \small Approximate Entropy             & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
     \small Random Excursions               & \Circle               & \CIRCLE               & \Circle            & \CIRCLE     \\
     \small Random Excursions Variant       & \Circle               & \CIRCLE               & \Circle            & \CIRCLE     \\
     \small Serial                          & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
     \small Linear Complexity               & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
    \bottomrule
    \end{tabular}
    }
\end{table}


\section{Computational complexity}\label{sec:complexity}\todo{change p and m into m and t (verify if no conflict with other sections)}

%Our prototype implementation is available on \href{https://github.com/AurelienCha/Decentralized-Sphinx}{GitHub}\footnote{\url{https://github.com/AurelienCha/Decentralized-Sphinx}}.
Our implementation, which will be released under the GPL license, is written in Python 3.13 using the \href{https://pypi.org/project/ECPy/}{ECPy}\footnote{\url{https://pypi.org/project/ECPy/}} library for elliptic curve operations. \todo{anonymous github link}
ECPy is well-suited for prototyping due to its simplicity, but it is significantly slower than many EC python libraries (approximately one order of magnitude slower). 
However, it is one of the few Python libraries that support Montgomery Curve25519, which is essential for Elligator.
Thus, rather than measuring raw execution time, we count the number of cryptographic operations, which are the computationally most expensive part of the scheme. 

Table~\ref{tab:operation_count} summarizes this asymptotic counting for each party, considering a path length of $p$ and $m$ semi-trusted third parties (STTPs).  
The total cost of sending a single message can be obtained by multiplying the cost per STTP by $m$ and the cost per mixnode by $p$.
\begin{table}%[t]
\centering
\caption{Asymptotic number of cryptographic operations per party, where $p$ is the path length and $m$ the number of STTPs.}
\label{tab:operation_count}
    \begin{tabular}{lccc}
        \toprule
        & \textbf{Client} & \textbf{STTP} & \textbf{Mixnode} \\
        \midrule
        \textbf{EC Multiplication}              & \bigO{p \, m} & \bigO{p^{2}}  & \bigO{p} \\
        \textbf{EC Addition}                    & \bigO{p \, m} & \bigO{p^{2}}  & \bigO{p} \\
        \textbf{Elligator Mapping}              & \bigO{p}      & \slash        & \slash \\
        \textbf{Inverse Mapping}                & \bigO{p}      & \slash        & \bigO{1} \\
        \textbf{Hash}                           & \bigO{p}      & \bigO{p^{2}}  & \bigO{p} \\
        \bottomrule
    \end{tabular}
\end{table}

The client bears most of the overhead introduced by STTPs, performing \bigO{p m} elliptic-curve multiplications and additions to split $p$ addresses and cryptographic secrets into $m$ shares.  
For each mixnode, the client also executes one Elligator mapping (to encode its IP) and one inverse mapping (Eq.~\ref{eq:shared_secret}), all of which scale linearly with $p$. % TODO (to encode its IP) -> Eq. X

For each packet layer, a STTP performs EC multiplications and additions (Eq.~\ref{eq:last_layer}, Eq.~\ref{eq:layer_i}), and hashes (Eq.~\ref{eq:integrity}) proportional to the number of chunks.  
As both the number of chunks and layers increase linearly with $p$, the total computational cost per STTP scales quadratically as $\mathcal{O}(p^2)$.  
However, since STTPs operate in parallel, their collective workload has limited impact on the overall latency.

Each mixnode performs EC multiplications, additions and hashes proportional only to the number of chunks (Eq.~\ref{eq:integrity_check}, Eq.~\ref{eq:decrypt}), yielding linear complexity in $p$.  
Additionally, each mixnode executes one Elligator inverse mapping (Eq.~\ref{eq:derive_secret}) to convert the shared secret point into an integer.


In practice, most mixnet implementations employ routes of three to five mixnodes, making \bigO{p} $\approx$ \bigO{1}.
Accordingly, the overall computational burden is more sensitive to the number of STTPs $m$ than to the path length $p$.
As our design remains secure as long as one STTP is honest, only a small number $m$ may be sufficient.
Determining the precise number needed to meet a desired trust threshold is left for future work.
\newline


A direct comparison with the original Sphinx protocol is not entirely meaningful, as some primitives differ (e.g. Elligator).  
Nevertheless, from a performance perspective, our protocol is expected to be slower than the original Sphinx header construction.  
The main contributing factors are: (1) the extensive use of elliptic curve arithmetic, in contrast to the lightweight hashing and XOR operations in Sphinx; and (2) the distributed nature of the header construction, which introduces communication latency.  
However, the computational load on individual mixnodes remains comparable to Sphinx, and most of the added overhead is mitigated by the parallel execution of STTPs.  

Given that mixnets inherently trade latency for privacy, the additional cost introduced by our decentralized design is reasonable.  
In return, it provides stronger flexibility for decentralized anonymous communication.


%%%%%%%%%%%%%%%%%%%%%%%%%%
%% MAYBE FOR CONCLUSION %%
%%%%%%%%%%%%%%%%%%%%%%%%%%

% However, we emphasize that a thorough formal security analysis remains necessary to fully assess the protocol's security, which we leave for future work.

% At this stage, our decentralized scheme successfully achieves three key
% security and privacy objectives as outlined in Section~\ref{sec:sp-objectives}.
% First, it maintains strong unlinkability, comparable to the original Sphinx.
% Second, integrity protection is verified at each mixnode, ensuring that message tampering can be reliably detected.
% Third, our scheme offers robustness against collusion among STTPs, as long as at least one remains honest, preventing them from learning any information about routing information or cryptographic secrets.
% Since credential issuance, modification, and verification are left for future work, the verifiability of legitimate traffic at each mixnode has not yet been achieved.
% However, a promising approach involves having STTPs update credentials during header construction, with mixnodes performing further updates during packet processing.


% While our scheme introduces higher computational complexity compared to the original Sphinx, it maintains a reasonable polynomial bound, specifically a quadratic complexity.
% This makes it viable for real-world applications, especially considering the additional features it could enable.
% The decentralized design could simplify future implementation of advanced features, such as a mechanism to verify the legitimacy of traffic at any mixnode.

% Our decentralized construction exhibits a higher computational cost than the original Sphinx, primarily due to distributed operations across STTPs.
% However, as these computations are parallelized, the resulting latency increase remains acceptable within the mixnet design trade-off between privacy and performance.
