\section{Evaluation}\label{sec:eval}
\todo[inline, color=pink]{
    Change 'Non-periodic Template Matching' to 'Non-overlapping Template Matching' in all figures.
    NIST uses the first name in their code, and the second name in their documentation.
}

Our prototype implementation is available on \href{https://github.com/AurelienCha/Decentralized-Sphinx}{GitHub}\footnote{\url{https://github.com/AurelienCha/Decentralized-Sphinx}}.
The implementation is written in Python 3.13 using the \href{https://pypi.org/project/ECPy/}{ECPy}\footnote{\url{https://pypi.org/project/ECPy/}} library for elliptic curve operations. 
ECPy is well-suited for prototyping due to its simplicity, but it is significantly slower than many EC python libraries (approximately one order of magnitude slower). 
Nevertheless, it remains one of the few Python libraries supporting Montgomery Curve25519, which is essential for our implementation.

We evaluate our system on two main axes: computational complexity and unlinkability (i.e. resistance to correlation between incoming and outgoing packets).

\subsection{Computational Complexity}

Instead of measuring raw execution time, which can vary significantly based on hardware and software environments, we focus on the number of expensive cryptographic operations. 
Table \ref{tab:operation_count} count these operations per party for $ m $ TTPs and path of length $ p $. 
To compute the total cost for sending a single message, multiply the TTP cost by $ m $ and the mixnode cost by $ p $. 
In typical mixnet implementations, the path includes approximately three to five mixnodes.
As a result, \bigO{p} can be considered constant (i.e. \bigO{1}).

\begin{table}
\caption{Number of expensive cryptographic operations per party, where $ p $ is the path length (e.g. $ p = 3 $), and $ m $ is the number of TTPs.}
\label{tab:operation_count}
    \begin{tabular}{lccc}
        \toprule
        & \textbf{Client} & \textbf{TTP} & \textbf{Mixnode} \\
        \midrule
        \textbf{EC Multiplication}              & \bigO{p \, m} & \bigO{p^{2}}  & \bigO{p} \\
        \textbf{EC Addition}                    & \bigO{p \, m} & \bigO{p^{2}}  & \bigO{p} \\
        \textbf{Point $\rightarrow$ Integer}    & \bigO{p}      & 0             & 2 \\
        \textbf{Integer $\rightarrow$ Point}    & \bigO{p}      & 0             & 0 \\
        \textbf{Hash}                           & \bigO{p}      & \bigO{p^{2}}  & \bigO{p} \\
        \bottomrule
    \end{tabular}
\end{table}

Consequently, the overall computational burden is more sensitive to the number of TTPs $ m $ rather than the path length $ p $. 
Since our design remains secure as long as a single TTP is honest (i.e. does not collude), only a small number of TTPs may be required. 
A deeper analysis of how many TTPs are needed to ensure a desired level of trust remains an open question for future work.


\subsection{Unlinkability Evaluation}
Quantifying unlinkability is inherently challenging. 
In Loopix~\cite{piotrowska2017loopix}, the authors introduced a probabilistic metric \textit{expected difference in likelihood} in order to quantify the \textit{sender-receiver third-party unlinkability}. 
However this relies on network simulation and does not quantify the unlinkability between packets, meaning under an adversary who is trying to correlate incoming and outgoing packets through cryptanalysis. 
Therefore unlinkability can be approximated by assessing the statistical randomness of the headers.
\todo[inline, color=yellow]{rephrasing 2 last sentences ?}

For this purpose, we rely on the NIST SP 800-22 statistical test suite \cite{NIST-SP80022}, which consists of 15 tests designed to evaluate the quality of random number generators. 
Each test produces a p-value representing how likely the data could be produced by a uniform source. 
If the data are truly random, the distribution of p-values across many samples must follow a uniform distribution.

We compare the statistical results (with default parameters) of our implementation with those of the original Sphinx implementation by Danezis\footnote{\href{https://github.com/UCL-InfoSec/sphinx}{https://github.com/UCL-InfoSec/sphinx}}.
All graphs presented in this section correspond to the results of the NIST statistical test suite applied to 20 distinct datasets.
Each dataset is generated under a different setup configuration to assess the variability and robustness of our implementation.
To generate a dataset, we simulate a network of 20 mixnodes and 3 trusted third parties (TTPs). 
For each iteration, a random destination IP address and a path consisting of 3 mixnodes are selected. 
The client then generates shares and distributes them to the three TTPs, which independently compute partial headers. 
These headers are then aggregated and forwarded through the simulated mixnet.
According to the NIST documentation, the statistical test suite requires input sequences of $1\,000\,000$ bits. 
To meet this requirement, generated headers are concatenated until the total length reaches $1\,000\,000$ bits. 
Each dataset contains 100 of those concatenated sequences.

NIST outlines two approaches for interpreting the empirical results of its statistical test suite.
The first evaluates the proportion of sequences that pass each statistical test, as shown in Fig.\ref{fig:proportion}.
The second evaluates whether the distribution of p-values is statistically uniform, as would be expected from random sequences.
These results are presented in Fig.\ref{fig:chisquare}.

\begin{figure}
    \centering
    % \hspace*{-1cm}
    \includegraphics[width=1.\linewidth]{Images/proportion_dot.png}
    \caption{Proportion of successful sequences from our implementation and the original Sphinx implementation over the fifteen NIST SP 800-22 statistical tests (significance level = $0.01$).
    \newline - TODO bigger dot + change 'Non-periodic' name}
    \label{fig:proportion}
\end{figure}

Figure \ref{fig:proportion} shows the proportion of sequences that successfully pass each statistical test for both our implementation and the original Sphinx implementation.  
Following the NIST guidelines, the significance level is set to $ \alpha = 0.01 $, and the confidence interval is defined as:
$$
(1 - \alpha) \pm 3 \sqrt{\frac{\alpha (1 - \alpha)}{m}},
$$
where $ m $ denotes the number of samples.

Our implementation fails only three tests: \textit{Non-overlapping Template Matching}, \textit{Random Excursions}, and \textit{Random Excursions Variant}, all of which are also failed by the original Sphinx implementation.  
In contrast, our implementation successfully passes three tests that the original implementation fails: \textit{Frequency}, \textit{Cumulative Sums}, and \textit{Overlapping Template Matching}.

\begin{figure*}[ht]
    \centering
    % \hspace*{-2cm}
    \includegraphics[width=1.\linewidth]{Images/hist_p-values.png}
    \caption{P-value distribution comparison between our implementation and the original Sphinx across the fifteen NIST SP 800-22 statistical tests.
    \newline - TODO: Change 'Nonperiodic' name}
    \label{fig:hist_p-values}
\end{figure*}

Figure \ref{fig:hist_p-values} presents the distribution of p-values obtained for each test.
A Chi-square test is performed on each of these histograms to assess their uniformity.
The results are shown in Figure \ref{fig:chisquare}, with a significance threshold of $0.0001$ as recommended by the NIST guidelines.
Two Chi-square results stand out: the \textit{Frequency} and \textit{Cumulative Sums} tests.  
Both were successful for our implementation, and failing for the original Sphinx implementation.

\begin{figure*}[ht]
    \centering
    % \hspace*{-1cm}
    \includegraphics[width=1.\linewidth]{Images/chisquare.png}
    \caption{Chi-square test assessing the uniformity of p-value distributions for each dataset across the fifteen NIST SP 800-22 statistical tests.
    \newline - TODO: Change 'Nonperiodic name}
    \label{fig:chisquare}
\end{figure*}

To better interpret our results, we begin with a brief overview of the specific NIST SP 800-22 tests involved (for further definitions, see the NIST documentation \cite{NIST-SP80022}).
The \textit{Frequency} test evaluates the proportion of zeros and ones across the entire sequence. 
It determines whether the number of ones and zeros is approximately equal, as expected in a truly random sequence. 
The \textit{Template Matching} tests examine how often predefined m-bit binary patterns appear within the sequence (by default $ m = 9 $). 
These tests aim to detect excessive repetitions of specific aperiodic patterns that could indicate some hidden structure.
In the \textit{Overlapping} version, a sliding window of $ m $ bits is shifted from a single bit at each step, allowing overlapping matches.
In the \textit{Non-overlapping} version, the window shifts from $ m $ bits in case of matching, avoiding overlapping matches.
The \textit{Cumulative Sums} test transforms the binary sequence of $1$ and $0$ respectively into a sequence of $+1$ and $–1$.
Then a cumulative sum is iteratively computed, interpreting the result as a one-dimensional random walk, \enquote{randomly} going up ($+1$) and down ($-1$).
A truly random walk is expected to oscillate around zero. 
The test evaluates how far this random walk moves away from zero and compares it with a truly random walk.
The \textit{Random Excursions} tests also rely on the random walk model derived from the \textit{Cumulative Sums} test. 
The \textit{Random Excursions} test counts the number of times each state has been visited within each cycle\footnote{A cycle is a segment of the walk between two successive zero state (state where the cumulative sum equals $0$).} 
and compares this distribution with theoretical expectations for a true random walk.
The \textit{Random Excursions Variant} test instead counts the total number of times each state has been visited over the entire walk.

In brief, the \textit{Frequency} evaluates the overall balance between zeros and ones, 
while the \textit{Cummulative Sums} and \textit{Random Excursions} tests brings additional information about their repartition,
and finally the \textit{Matching Template} tests look for excessive repetition of substrings.


\subsection{Discussion}

In terms of computational complexity, our implementation is slower than the original Sphinx implementation.
However, most of the additional computational workload is distributed among the TTPs, which operate in parallel. 
Given that mixnets inherently tolerate some latency, this overhead is likely acceptable in practice.


Regarding unlinkability (i.e. output randomness), our decentralized implementation performs better than the original implementation, as it passes a greater number of statistical tests, as summarized in Table~\ref{tab:test_summary}.
The results suggest that both implementations may exhibit subtle patterns or hidden structure, as indicated by failures in the \textit{Random Excursions} and the  \textit{Non-overlapping Template Matching} tests. 
This behavior could potentially comes from the underlying mathematical framework (e.g. elliptic curves), which is not inherently truly random.
Nevertheless, our implementation demonstrates a better overall balance between ones and zeros, as reflected in the \textit{Frequency} and \textit{Cumulative Sums} test results.
Given that the original Sphinx implementation is widely regarded as secure, and considering the similarities in core structure, our results indicate that our scheme is a solid starting point toward achieving similar privacy guarantees.
However, we emphasize that a thorough formal security analysis remains necessary to fully assess the protocol's security, which we leave for future work.

\begin{table}
\caption{Statistical test summary for both implementations (\CIRCLE~=~Pass, \Circle~=\enspace Fail)}
\label{tab:test_summary}
    \scalebox{0.9}{
    \begin{tabular}{|l|cc|cc|}
    \toprule
                                    & \multicolumn{2}{c|}{\textbf{Decentralized}}   & \multicolumn{2}{c|}{\textbf{Original}} \\
    \cline{2-5}
                                    & \textbf{\tiny Excessive}    & \textbf{\tiny Lacks}        & \textbf{\tiny Excessive} & \textbf{\tiny Lacks} \vspace*{-1.5mm} \\
    \textbf{Statistical Test}       & \textbf{\tiny Rejections}   & \textbf{\tiny Uniformity}   & \textbf{\tiny Rejections} & \textbf{\tiny Uniformity} \\
    \midrule
    Frequency                       & \CIRCLE               & \CIRCLE               & \Circle            & \Circle     \\
    Block Frequency                 & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
    Cumulative Sums                 & \CIRCLE               & \CIRCLE               & \Circle            & \Circle     \\
    Runs                            & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
    Longest Run                     & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
    Rank                            & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
    Discrete Fourier Transform      & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
    Non-overlapping Template Match.  & \Circle               & \CIRCLE               & \Circle            & \CIRCLE     \\
    Overlapping Template Match.  & \CIRCLE               & \CIRCLE               & \Circle            & \CIRCLE     \\
    Universal Statistical           & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
    Approximate Entropy             & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
    Random Excursions               & \Circle               & \CIRCLE               & \Circle            & \CIRCLE     \\
    Random Excursions Variant       & \Circle               & \CIRCLE               & \Circle            & \CIRCLE     \\
    Serial                          & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
    Linear Complexity               & \CIRCLE               & \CIRCLE               & \CIRCLE            & \CIRCLE     \\
    \bottomrule
    \end{tabular}
    }
\end{table}

At this stage, our decentralized scheme successfully achieves three key security and privacy objectives.
First, it maintains strong unlinkability, comparable to the original Sphinx.
Second, integrity protection is verified at each layer, ensuring that message tampering can be reliably detected.
Third, our scheme offers robustness against collusion among TTPs, as long as at least one remains honest, preventing them from learning any information about routing information or cryptographic secrets.

While our scheme introduces higher computational complexity compared to the original Sphinx, it maintains a reasonable polynomial bound, specifically a quadratic complexity.
This makes it viable for real-world applications, especially considering the additional features it could enable.
The decentralized design could simplify future implementation of advanced features, such as a mechanism to verify the legitimacy of traffic at any mixnode.

