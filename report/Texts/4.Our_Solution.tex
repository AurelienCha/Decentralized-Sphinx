\section{Protocol Description}\label{sec:scheme}

% Overview
The proposed decentralized protocol distributes path sampling across a threshold number of semi-trusted third parties (STTPs), in order to mitigate client-side route biasing attacks. 
Fig.~\ref{fig:overall_schema} illustrates the protocol, which is divided into three phases: 
a one-time setup phase (Step 0), a decentralized path sampling phase (Steps 1-3), and a header construction phase (Step 4).

\begin{figure}%[t]
    \centering
    \resizebox{0.9\linewidth}{!}{\input{schema/schema_overview.tex}}
    \caption{Overview of decentralized path selection.
    (0) Setup, (1) client nonce, (2) path sampling, (3) aggregation, (4) header construction, (5) packet forwarding.}
    \label{fig:overall_schema}
\end{figure}

\noindent\textbf{DSphinx header.} 
% Header structure - Chunks
The current protocol is based on an adaptation of the Sphinx packet format that we called DSphinx.
The key distinction is that each header element is independently encoded as an elliptic curve (EC) point. 
Consequently, the DSphinx header is structured as a sequence of fixed-size chunks, as illustrated in Fig.~\ref{fig:chunkwise-overview}, where each chunk corresponds to a single header element represented by an EC point.
Each network node along the packet route contributes for two chunks: one encoding its address and another encoding a per-hop integrity tag. 
Therefore, a route of length $m$ is represented by $n = 2m + 1$ chunks, accounting for two chunks per network node and one additional chunk corresponding to the final destination.
Some routing information, such as IP addresses, must be reversibly encoded as elliptic curve points. 
To this end, DSphinx uses the Elligator mapping~\cite{bernstein2013elligator}, which produces points computationally indistinguishable from randomly sampled group elements.

\smallskip
\noindent\textbf{Design rationale.}
DSphinx decentralizes route sampling across multiple STTPs to prevent both clients and STTPs from manipulating path selection.
Clients contribute only a nonce $w$, which is deterministically mapped to the curve point $\A{}$ using a hash-to-curve function.
The routing path is then deterministically derived from the tuple $(\A{},\text{timestamp},i)$, where the timestamp ensures different sampled paths over time, and the hop index $i$ ensures independent node selection across hops.
Since hash-to-curve is non-invertible, influencing the sampled route reduces to repeated trial over the nonce space.
Likewise, all honest STTPs execute the same deterministic procedure and therefore produce identical routing decisions from the same nonce. 
Any deviation results in inconsistent shares that cannot be reconstructed by the client, which queries at least d STTPs and reconstructs the route from any consistent subset of d responses. 
Furthermore, STTPs observe only node pseudonyms and therefore cannot associate sampled routes with physical nodes without additional collusion.


\subsection{Setup}

Each network node $i$ samples a random identifier $r_i$ and constructs two random polynomials $f_i$ and $g_i$ of degree $d-1$, such that:
$f_i(0) = k_i$ and $g_i(0) = N_i$, where $k_i$ denotes the node's secret key, $N_i$ its encoded address in $\mathbb{G}$, and $d$ the reconstruction threshold.

Each STTP is identified by a value $t$, computed as the hash of its address. 
For each STTP $t$, network node $i$ evaluates $f_i(t)$ and $g_i(t)$ and securely transmits the corresponding shares together with $r_i$ to the STTP $t$ as in \eqref{eq:sttp_share}.
We assume these shares are transmitted anonymously, preventing STTPs from linking them to specific nodes. 
Each STTP stores the received shares indexed by $r_i$, which later serves to pseudorandomly select network nodes during route sampling.
\begin{equation}
    (r_i,\quad k_{it}=f_i(t),\quad N_{it}=g_i(t))
    \label{eq:sttp_share}
\end{equation}

Adding a new node only requires executing the setup procedure for that node. 
Adding a new STTP requires each node to compute and distribute an additional polynomial evaluation. 
However, changing the threshold $d$ requires reinitializing the entire setup.

\subsection{Path Sampling}\label{sec:path_sampling}

To sample a routing path, the client sends a nonce $w$ to at least $d$ STTPs. 
Each STTP maps this nonce to the curve point $\A{}$ using a hash-to-curve function.

For each hop $i$, the STTP computes $r'_i = \text{hash}\!\left(\A{}, \text{timestamp}, i\right)$, and selects the network node share $(r_i, k_{it}, N_{it})$ whose identifier $r_i$ is closest to $r'_i$.
The STTP then computes the partial Diffie-Hellman shared secret $S_{it} = k_{it}\A{}$, 
and returns its identifier together with the selected shares and partial shared secrets to the client:
\begin{equation*}
    \left(t,\; \{N_{it}\}_{_{i=1}}^{^{m}},\; \{S_{it}\}_{_{i=1}}^{^{m}}\right)
\end{equation*}
Note that the path sampling scheme proposed in this work is designed for uniform random selection of routing nodes. 
While the Lightning Network implements variants of shortest-path algorithms such as LND~\cite{lnd2020}, eclair~\cite{eclair2020} and c-lightning~\cite{clightning2020}, prior research has shown that random route selection offers stronger privacy guarantees~\cite{sharma2022anonymity,kumble2021lightning,tochner2019hijacking}. 
Similarly, in mixnet-based systems, the routing strategy that maximizes privacy is uniform random selection of mixes across layers~\cite{benguirat2022mixnet,benguirat2021mixim}. 
Hence, this work focuses on random path sampling, leaving alternative routing algorithms for future work.
\subsection{Aggregation}

Upon receiving responses from at least $d$ STTPs, the client reconstructs node addresses and shared secrets via Lagrange interpolation as in \eqref{eq:lagrange_reconstruction},
where $\lambda(t)$ denotes the Lagrange coefficient for STTP identifier $t$.
Since all STTPs process the same nonce $w$, the reconstructed values correspond to a consistent path.
\begin{equation}
    N_i = \sum_t \lambda(t) N_{it}, \qquad
    S_i = \sum_t \lambda(t) S_{it},
    \label{eq:lagrange_reconstruction}
\end{equation}

To prevent linkability, the client samples a random scalar $r$ to rerandomize the shared secrets and updates the cryptographic element $\A{}$.
Those shared secrets are derived iteratively in cascade, where each hop incorporates previous secrets:
\begin{equation*}
    s_i = \text{HashToScalar}\!\left(r S_i \prod_{j=1}^{i-1} s_j \right).
    \label{eq:cascade_secret}
\end{equation*}


\subsection{Header Construction}
Given the reconstructed route $\{N_i\}_{i=1}^{m}$ and per-hop shared secrets $\{s_i\}_{i=1}^{m}$, the client constructs the DSphinx header for destination $\D{}$ (encoded in $\mathbb{G}$).
The header construction proceeds in two phases; 
a preprocessing phase (Alg.~\ref{algo:preprocessing}, Fig.~\ref{fig:chunk-preprocessing}) that generates filler chunks, 
followed by a layer-by-layer encryption phase (Alg.~\ref{algo:sttp}, Fig.~\ref{fig:chunk-encryption}) applied in reverse order, from the last hop to the first.
To ensure unlinkability, encryption is performed using a set of independently derived generators $\{G_j\}_{j=1}^{n}$, one per chunk in the header, such that no scalar relationship between any pair of generators is known.

% Preprocessing
\smallskip
\noindent\textbf{Preprocessing Phase.}
The preprocessing phase prepares a vector of filler chunks $\phi$ according to \eqref{eq:preprocessing}.
These filler chunks ensure that after each encryption layer the last two chunks evaluate to the identity element and can thus be safely truncated, ensuring a constant header size and reversibility when processed by the network nodes. 
The detailed procedure is specified in Alg.~\ref{algo:preprocessing}, which takes as input the set of shared secrets $\{s_i\}$ and the set of chunk generators $\{G_j\}$, and outputs $n-1$ filler chunks, where $n = 2m+1$ is the header size for a path of length $m$. 
An example for $m=3$ is illustrated in Fig.~\ref{fig:chunk-preprocessing}.
\begin{equation}
    \phi_j = - \sum_{i=1}^{m} s_i\, G_{\,n+j-2i}, \qquad 1 \le j \le n-1 .
\label{eq:preprocessing}
\end{equation}

% Layers
\noindent{\textbf{Layered Encryption Phase.}}
The header is initialized by concatenating the destination chunk $\D{}$ with the filler chunks $\phi$, producing a $n$-chunks header.
Encryption is then applied iteratively in reverse path order, from $i=m$ down to $i=1$, 
as illustrated in Fig.~\ref{fig:chunk-encryption} and detailed in Alg.~\ref{algo:sttp}.
\begin{enumerate}
    \item \textbf{Chunk encryption:}
    Each chunk $j$ is encrypted according Eq.~\eqref{eq:layer_i}.
    The last two chunks evaluate to the identity element due to the preprocessing phase and may therefore be discarded while preserving subsequent processing.
    \begin{equation}
        H_{ij} = H_{(i+1,\,j)} + \sG{i}{j}
        \label{eq:layer_i}
    \end{equation}

    \item \textbf{Integrity computation:} Remaining chunks are used for the integrity tag: 
    \begin{equation}
        \C{i} =  \text{HMAC}(\B{i}{}, s_i)
        \label{eq:integrity}
    \end{equation}

    \item \textbf{Prepend Node:}
    The node address $\N{i}$ and the integrity tag $\C{i}$ are prepended to $H_i$, producing again a $n$-chunks header.
\end{enumerate}

%\newpage
\vspace*{-4mm}
\begin{figure}[H]
    \centering
    % -------- Left figure (b) --------
    % \setcounter{sub\@captype}{1}
    \begin{subfigure}[b]{0.48\linewidth}
        \setcounter{subfigure}{1} % forces next to be (b)
        % \centering
        \resizebox{\linewidth}{!}{\input{Appendices/chunkwise_encryption.tex}}
        \caption{Encryption (client)}
        \label{fig:chunk-encryption}
    \end{subfigure}
    \hfill
    % -------- Right column --------
    \begin{subfigure}[b]{0.48\linewidth}
        % \centering

        % (a) top-right
        \begin{subfigure}[t]{\linewidth}
            \setcounter{subfigure}{0} % forces next to be (a)
            \centering
            \resizebox{\linewidth}{!}{\input{Appendices/chunkwise_preprocessing.tex}}
            \caption{Preprocessing (client)}
            \label{fig:chunk-preprocessing}
        \end{subfigure}

        \vspace{1.em}

        % (c) bottom-right
        \begin{subfigure}[b]{\linewidth}
            \setcounter{subfigure}{2} % forces next to be (c)
            \centering
            \resizebox{\linewidth}{!}{\input{Appendices/chunkwise_decryption.tex}}
            \caption{Decryption (network node)}
            \label{fig:chunk-decryption}
        \end{subfigure}

    \end{subfigure}

    \caption{Overview of chunk-wise header processing for a path of length $m=3$. Circled numbers \raisebox{1pt}{\eq[\color{red}\scriptsize x]} refers to Equation x in the paper.}
    \label{fig:chunkwise-overview}
\end{figure}
\vspace*{-17mm}
\begin{figure}[H]
    \centering
    \begin{minipage}{0.47\linewidth}
        \begin{algorithm}[H]
        \small
        \input{schema/code_filler_chunks_small.tex}
        \label{algo:preprocessing}
        \end{algorithm}
    \end{minipage}
    \hfill
    \begin{minipage}{0.47\linewidth}
        \begin{algorithm}[H]
        \small
        \input{schema/code_sttp_small.tex}
        \label{algo:sttp}
        \end{algorithm}
    \end{minipage}
\end{figure}



\subsection{Network Nodes Processing}
Upon receiving a packet, node $i$ extracts the cryptographic element $\A{i}$, the integrity tag $\C{i}$, and the encrypted routing information $\B{i}{}$. 
As in the original Sphinx design, the node checks whether $\A{i}$ has been previously observed since its last key rotation and discards duplicates to prevent replay attacks.

The header is processed as illustrated in Fig.~\ref{fig:chunk-decryption}.
The node first derives the shared secret $\s{i}$ by multiplying the cryptographic element $\A{i}$ with its private key $\sk{i}$, and then hashing the resulting point into a scalar as in \eqref{eq:derive_secret}.
The shared secret $\s{i}$ is then used to verify the integrity tag in \eqref{eq:integrity_check}.
If verification succeeds, the node appends two identity chunks and decrypts the header as in \eqref{eq:decrypt}. 
The first decrypted chunk $\BB{i}{1}$ reveals the next hop $\N{i+1}$. 
The second chunk $\BB{i}{2}$ contains the integrity tag $\C{i+1}$ for the next hop, while the remaining chunks form the next encrypted routing information $\B{i+1}{}$.
Finally, the node updates the cryptographic element according to \eqref{eq:update_alpha} and forwards the packet to the next hop.
\begin{align}
\s{i} &= \text{HashToScalar}\!\left(\sk{i} \, \A{i}\right) \label{eq:derive_secret} \\
\C{i} &\overset{\text{?}}{=} \text{HMAC}\!\left(\B{i}{}, \s{i}\right) \label{eq:integrity_check} \\
\BB{i}{j} &= \B{i}{j} - \sG{i}{j} \label{eq:decrypt} \\
\A{i+1} &= s_i \, \A{i} \label{eq:update_alpha} 
\end{align}